# fantasy-analysis
A simple repo to graph correlations between fun things before a fantasy draft

## Running
- `yarn` or `npm install`
- `yarn nodemon server`
- Go to `localhost:3000/receiving`, chech the server file for more routes
